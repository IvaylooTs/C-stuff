extern - вкарва сорс файл във хеадър

<access> <type return(типа на резултата)> име на функцията <аргументи>;

void A(); най простата декларация на функция - когато скобите са празни в Ц++ може да е проблем
затова винаги пишем резултат във скобите 
- void A(void);

int
char
struct B
U32
typedef unsigned int U32; - чрез tupedef може да си дефинираме сами резултат

