#include <stdio.h>
#include <upperlower.c>


int main()
{
    char lower, upper = upperchar(lower);
    printf("Enter character: ");
    scanf("%c", %lower);
    printf("Your %c upper char is %c", lower, upper);
    return 0;
}
